# Copy to keystore.properties (gitignored) on the Mac that holds the upload key.
# storeFile is relative to native/android/.
storeFile=../../secrets/korpasset-upload.jks
storePassword=
keyAlias=korpasset
keyPassword=
